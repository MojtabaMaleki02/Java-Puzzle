package puzzle.model;

/**
 * The {@code Square} enum represents the different types of squares in the puzzle game.
 * It defines the various states that a square on the game board can have.
 */
public enum Square {
    NONE,
    RED,
    BLUE,
    BLOCK
}
