/**
 * This package contains classes related to the controller logic of the puzzle game application.
 * It includes the main entry point {@link puzzle.controller.PuzzleApplication} class,
 * which extends {@link javafx.application.Application} to set up the main application window.
 *
 * <p>The application initializes the primary stage with a fixed-size scene, loads the FXML file
 * to create the user interface, and displays the main window.</p>
 *
 * @since 1.0
 */
package puzzle.controller;
